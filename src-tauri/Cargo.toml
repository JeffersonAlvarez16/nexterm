[package]
name = "nexterm"
version = "0.2.2"
description = "NexTerm — Cross-platform SSH client with terminal, SFTP, and tunneling"
authors = ["you"]
edition = "2021"

[lib]
name = "nexterm_lib"
crate-type = ["lib", "cdylib", "staticlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
# Tauri
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"

# SSH — russh 0.50 merged russh-keys into the main crate; russh-sftp 2.1.1 is
# compatible with russh 0.50.x (its dev-dep targets 0.51).  Direct ssh-key dep
# is dropped: russh 0.50 bundles an internal fork of ssh-key 0.6.9; we reference
# all key types via russh::keys::ssh_key::* to avoid type-mismatch with the fork.
russh = "0.50"
russh-sftp = "2.1.1"

# Async runtime
tokio = { version = "1", features = ["full"] }
tokio-util = { version = "0.7", features = ["rt"] }

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Error handling
thiserror = "2"

# UUID
uuid = { version = "1", features = ["v4", "serde"] }

# Date/time
chrono = { version = "0.4", features = ["serde"] }

# Encrypted credential vault
aes-gcm = "0.10"
argon2 = "0.5"
rand = "0.8"

# Open files with system default app
open = { version = "5", features = ["shellexecute-on-windows"] }

# Platform directories
dirs = "6"

# Cryptographic hashing (for host key fingerprints)
sha2 = "0.10"
base64 = "0.22"
# HMAC-SHA1 for matching OpenSSH hashed known_hosts entries (|1|salt|hash)
hmac = "0.12"
sha1 = "0.10"

# Shell path expansion (~/ to home dir)
shellexpand = "3"

# Secure memory zeroization
zeroize = { version = "1", features = ["derive"] }

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# Unique temp file creation for atomic writes (known_hosts race fix)
tempfile = "3"

[target."cfg(not(any(target_os = \"android\", target_os = \"ios\")))".dependencies]
tauri-plugin-updater = "2"
tauri-plugin-process = "2"
# Single-instance lock: prevents two app instances from racing on the
# profiles.json / vault.json files (silent data loss). Desktop-only.
tauri-plugin-single-instance = "2"

# Windows-only — used by fs_secure::windows for owner-only NTFS DACLs.
# Already pulled transitively via tauri-plugin-updater; declared explicitly
# here because we use it directly. Zero binary-size impact.
[target.'cfg(windows)'.dependencies]
windows = { version = "0.61", features = [
  "Win32_Foundation",
  "Win32_Security",
  "Win32_Security_Authorization",
  "Win32_System_Memory",
  "Win32_System_Threading",
] }

[dev-dependencies]
